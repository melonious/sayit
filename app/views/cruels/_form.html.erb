<%= form_with model: @cruel, local: true do |form| %>
  <% if @cruel.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@cruel.errors.count, "error") %> prohibited this Cruel from being saved:
      </h2>
      <ul>
        <% @cruel.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <p>
    <%= form.label :message %>
    <%= form.text_area :message %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>